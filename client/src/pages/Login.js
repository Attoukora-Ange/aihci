import { Layout } from "../components/layout/Layout";
import { LoginInfo } from "../components/login/LoginInfo";

export const Login = () => {
  return (
    <Layout>
      <LoginInfo />
    </Layout>
  );
};
